# Preencher com os dados do projeto Supabase (feito no deploy, junto com o Gabriel).
# O app usa a chave PÚBLICA (anon/publishable). A OPENAI_API_KEY NÃO fica aqui —
# é segredo da Edge Function (supabase secrets set OPENAI_API_KEY=...).
EXPO_PUBLIC_SUPABASE_URL=https://SEU-PROJETO.supabase.co
EXPO_PUBLIC_SUPABASE_KEY=sua-chave-publica-anon-ou-publishable
